widgets = {
    ram = {
        view_offset = 0x0000,
        style = function(_) end,
    },
    registers = {
        aliases = {},
        visibility_bitmask = 0xFFFFFFFF,
        style = function(_) end,
    },
    control_status_registers = {
        aliases = {},
        visibility_bitmask = 0xFFFFFFFF,
        style = function(_) end,
    },
}
